name: volvero-emails
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
      - playwright==1.42.0
      - pandas>=2.0.0
      - numpy>=1.24.0
      - pytest>=7.4.0
      - openpyxl>=3.1.0
